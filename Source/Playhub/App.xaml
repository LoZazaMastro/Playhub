<Application
    x:Class="Playhub.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    RequestedTheme="Dark">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <controls:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>

            <Thickness x:Key="PlayhubPagePadding">36,24,36,64</Thickness>
            <CornerRadius x:Key="PlayhubCardCornerRadius">8</CornerRadius>
            <Thickness x:Key="PlayhubCardPadding">16</Thickness>

            <Style x:Key="PlayhubPageTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="32" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="TextWrapping" Value="NoWrap" />
            </Style>

            <Style x:Key="PlayhubSectionTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="PlayhubBodyTextStyle" TargetType="TextBlock">
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="Opacity" Value="0.78" />
                <Setter Property="LineHeight" Value="22" />
            </Style>

            <Style x:Key="PlayhubCardBorderStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="{StaticResource PlayhubCardCornerRadius}" />
                <Setter Property="Padding" Value="{StaticResource PlayhubCardPadding}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="Transitions">
                    <Setter.Value>
                        <TransitionCollection>
                            <EntranceThemeTransition FromVerticalOffset="10" />
                        </TransitionCollection>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PlayhubPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                <Setter Property="MinWidth" Value="116" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>

            <Style x:Key="PlayhubSecondaryButtonStyle" TargetType="Button">
                <Setter Property="MinWidth" Value="116" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
