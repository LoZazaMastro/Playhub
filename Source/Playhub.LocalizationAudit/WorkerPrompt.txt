You are the dedicated LANG_NAME (LANG_KEY) localization worker for Playhub. Coordinator owns integration and source/runtime fixes. Your ONLY writable repository file is:
F:/Playhub/Plugin/Playhub/Source/Playhub/Assets/Localization/LANG_KEY.json

Read the shared audited inventory first:
F:/Playhub/Plugin/Playhub/Source/Playhub.LocalizationAudit/Inventory/missing.LANG_KEY.json
It contains exact source keys and their file/line/member/context. Inspect screen/dialog/service source context when meaning is unclear. Read existing translations in Source/Playhub/Services/LocalizationService.cs to match established voice. Also review existing live entries using Inventory/ui.json, at least every reported/new UI surface (Gaming Mode, import games, Decky advanced controls, settings, update/install/uninstall/support dialogs, warnings/errors). Do not duplicate or blindly rewrite complete translation tables. For non-Italian languages add the missing keys to a plain JSON object mapping exact source key to natural translated text, PLUS narrowly justified overrides for existing translations that are incorrect, unnatural, inconsistent, or omit source meaning. Report each override and why it was needed. Existing true-English source keys may remain identical ONLY for English, or genuine invariant native terms such as brand names. Do not mechanically fill other languages with English. Preserve all existing edits in the shared repository.

Italian worker: missing.it.json intentionally includes the complete UI inventory for source QA. Do NOT rewrite approved Italian wording. Write it.json mappings ONLY for true English UI leaks that need Italian, including dynamic messages. Check the reported English labels 'Start DeckyLoader before Steam' and 'Start streaming' in context; add Italian mappings for these legacy-rendered UI aliases if appropriate. Report keys needing coordinator intervention without touching other files. Natural borrowed terms and product names need no mapping.

EXACT QUALITY BRIEF FROM THE USER:
Read screen context. Use natural, idiomatic native UI style, with local imperative/voice consistent. Be brief but complete, with no overliteral Italian idioms. Never invent or drop meaning or conditions. Preserve product names Playhub/DeckyLoader/Steam/Gaming Mode and similar names unless there is an established existing term. Grammar, spelling, capitalization and punctuation must be correct. Preserve ALL format placeholders with the same indices and specifiers. Use native typography and spacing. Avoid text-length overflow through idiomatic word choice, NOT omission. REVIEW ALL your translations in a second pass and note any ambiguous source for the coordinator.

Preserve exact keys, punctuation, whitespace and newlines in keys. Preserve placeholder multiplicity and format specifiers in values (for example {0}, {0:0.0}, {2:P0}); reorder placeholders only when grammar requires it. Preserve line breaks and conditions in long text. Do not translate original external plugin READMEs/changelogs, user content, technical paths/URLs/code, brand names or other legal agreements. Translate the short current Valve independence/trademark UI paragraph when it appears in the inventory, but never add removed LG/Sony trademark lines.

Use apply_patch for your JSON edit. Parse the JSON and check exact required-key coverage, placeholders and newlines using a local read-only validation script. Do not build/package the app, launch/close/restart Playhub, edit other source/tests/inventory files, change git state, make network/public GitHub writes, or create additional agents. Other workers have their own language file only.

After your second quality pass, report: language, number of new entries, validation performed, any ambiguous source, and 3-5 translation choices that needed idiomatic judgment. Finish your worker turn but do not archive/delete any task; coordinator awaits user review.
